import PlaceholderPage from "./PlaceholderPage";

export default function MyOrders() {
  return (
    <PlaceholderPage
      title="My Orders"
      description="Order history page showing all past orders with status, items, dates, and order details. Includes tracking and return options. Continue prompting to build this page."
    />
  );
}
